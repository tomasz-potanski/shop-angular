describe('Detail-view tests', function() {

  beforeEach( function () {
    browser.get( '/shop/item/2' );
  } );

  //it( 'Should contain zimage', function containZImage() {
  //  var listOfElements = $$( '.image' );
  //
  //  expect( listOfElements.count() ).toEqual( 1 );
  //} );

  //it( 'should display product name', function() {
  //  var el = $( 'span.name' );
  //
  //  expect( el.getText() ).toBeTruthy();
  //} );

  //it( 'should redirect back to shop', function() {
  //  var backButton = $( 'div.back-btn' );
  //  backButton.click();
  //
  //  browser.getLocationAbsUrl().then( function ( url ) {
  //        expect( url ).toEqual( '/shop' );
  //      } );
  //} );



});